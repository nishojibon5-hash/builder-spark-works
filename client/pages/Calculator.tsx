import PlaceholderPage from "./PlaceholderPage";

export default function Calculator() {
  return (
    <PlaceholderPage
      title="ঋণ ক্যালকুলেটর / Loan Calculator"
      description="Calculate your EMI, interest, and repayment schedule"
      suggestions={["/apply", "/loans"]}
    />
  );
}
